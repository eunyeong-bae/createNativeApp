import React, { useContext, useMemo } from "react";
// import { CommonContext } from "../context/CommonContext";
// import ActionMenu from "../menu/ActionMenu";
import CommonUtil from '../utils/CommonUtil';

export default function ActionMenuClass() {
    // const { actionMenuState, selectedTargetState } = useContext(CommonContext);
    
    // return useMemo(() => (
    //     <>
    //         { ( actionMenuState.isActionMenu && !CommonUtil.objectIsNull( selectedTargetState.selectedTarget)) &&
    //             <ActionMenu isActionMenu={actionMenuState.isActionMenu} 
    //                         setIsActionMenu={actionMenuState.setIsActionMenu}
    //                         contextName={actionMenuState.contextName}
    //                         navigation={actionMenuState.navigation}
    //             />
    //         }
    //     </>
    // ), [actionMenuState]);
}